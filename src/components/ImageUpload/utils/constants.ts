import { AspectRatios } from '../types';

export const cropperAreaWidthREM = 42;
export const cropperAreaHeightREM = 32;

export const mediaAllowedExtensions = ['jpg', 'jpeg', 'png', 'webp', 'gif'];
export const mediaBaseSize = 400;
export const mediaMinSize = mediaBaseSize * AspectRatios.ALBUM;
